<!-- navigationbar -->
<%- include('../includes/navigation.ejs') %>

    <!-- header -->
    <%- include('../includes/head.ejs') %>

        <!-- section-2 -->
        <section class="toursSec2">
            <div class="container">
                <div class="row d-flex justify-content-center pt-5 pb-5">
                    <div class="col-sm-10 col-lg-6 ">
                        <form id="form" method="post" action="/profile/personal/verification/<%= type %>/<%= user._id %>"
                            class="signupSec2Form">
                            <div class="mb-3 mt-2">
                                <label class="form-label signupSec2Text">Otp</label>
                                <input  type="number" class="form-control" name="otp"
                                    placeholder=" Enter Otp Number" required>
                            </div>
                            <div class="d-flex justify-content-center p-4">
                                <button type="submit" class="signupSec2FormButton">Submit</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </section>

        <!-- footer -->
        <%- include('../includes/end.ejs') %>